import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const BreakdownInfo = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View>
        <Text>Breakdown Info</Text>
      </View>
    </SafeAreaView>
  );
};

export default BreakdownInfo;
